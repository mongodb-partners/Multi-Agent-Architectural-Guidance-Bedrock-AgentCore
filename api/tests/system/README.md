# System tests (black-box)

Place scripts or tests here that spawn a real server process and call HTTP (e.g. repo `scripts/system-smoke.sh`). Bun tests in this folder should use a `*.system.test.ts` name pattern when added.
