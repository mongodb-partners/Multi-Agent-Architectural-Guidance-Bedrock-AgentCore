streamlit>=1.32,<2
requests>=2.31,<3
streamlit-cognito-auth>=1.3,<2
boto3>=1.34,<2
pytest>=8,<10
# OpenTelemetry — auto-instruments `requests`, `urllib3`, and Streamlit's
# Tornado server so HTTP server spans + outbound API calls land in the same
# /aws/spans trace tree as the Bun API. `opentelemetry-distro` resolves a
# matching set of api/sdk versions and provides the `opentelemetry-instrument`
# launcher used in the Dockerfile CMD. The exporter speaks OTLP/HTTP to
# 127.0.0.1:4318 — the ADOT Collector sidecar signs SigV4 outbound.
# NOTE: opentelemetry-distro / -instrumentation versions track the same 0.NNbX
# pre-release stream as the core OTel API (0.62b1 as of 2026-05). Keep the
# floor at 0.57 to match the Bun-side OTel pin in api/package.json, and the
# ceiling open so dependabot can move the cap forward together with the
# Python API/SDK pins.
# `b0` suffix is required: pip won't match pre-releases unless the specifier
# explicitly includes one. 0.57+ of the instrumentation packages are still
# released as 0.NNbX pre-releases by upstream.
opentelemetry-distro>=0.57b0
opentelemetry-exporter-otlp-proto-http>=1.29,<2
opentelemetry-instrumentation-requests>=0.57b0
opentelemetry-instrumentation-urllib3>=0.57b0
