# Status and operations

Operational status, debugging playbooks, and run-state notes for the deployed stack.

| Doc | Purpose |
|---|---|
| [`debugging.md`](debugging.md) | Developer playbook: EC2 access, log tailing, trace-driven debug, common failures, validation scripts, **known persistent pitfalls** |

When a non-obvious regression recurs more than twice (or is a severe guardrail), add an entry to **Known persistent pitfalls** in [`debugging.md`](debugging.md) in the same PR as the fix.
